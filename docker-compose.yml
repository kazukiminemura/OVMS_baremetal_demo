services:
  ovms:
    image: openvino/model_server:latest-gpu
    volumes:
      - ./models:/models
    devices:
      - /dev/dri:/dev/dri
    ports:
      - "8000:8000"
    command: --config_path /models/config.json --rest_port 8000
